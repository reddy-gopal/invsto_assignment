generator client {
  provider = "prisma-client-py"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model StockData {
  id        Int      @id @default(autoincrement())
  datetime  DateTime
  open      Float
  high      Float
  low       Float
  close     Float
  volume    Int
}
